<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.adnstyle.myboard.model.repository.JyUserRepository">

    <insert id="insertNewUser" parameterType="jyUser">
        insert into jy_user( user_id , user_pw, user_name , user_phone, user_email, user_birth, reg_date)
        values (#{userId},#{userPw},#{userName},#{userPhone},#{userEmail},#{userBirth}, NOW())
    </insert>

    <select id="checkId" parameterType="string" resultType="int">
        select count(*)
        from jy_user
        where user_id = #{userId}
    </select>

    <select id="checkEmail" parameterType="string" resultType="int">
        select count(*)
        from jy_user
        where user_email = #{userEmail}
    </select>

    <select id="selectUser" parameterType="string" resultType="String">
        select user_pw
        from jy_user
        where user_id = #{userId}
        and del_yn ='N'
    </select>
</mapper>